# Progetto Supabase reale collegato: Neros-kitchen1 (eu-central-2, org neros-kitchen).
# Questi due valori sono pubblici per design (protetti dalla RLS lato server, non da segretezza).

NEXT_PUBLIC_SUPABASE_URL=https://xjvrhoweghzfvwjsvwla.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InhqdnJob3dlZ2h6ZnZ3anN2d2xhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQ3MzU1NTMsImV4cCI6MjEwMDMxMTU1M30.brxyeVoRmeYNg0n9E5CdeW2aR0QBX_T6QL6lObOgb9Q

# Solo server-side (Edge Functions / route handler con service role) — MAI esporre al client.
# Non generata/condivisa da questa sessione per motivi di sicurezza: recuperala tu da
# Supabase Dashboard → Project Settings → API → service_role secret, e impostala solo
# come variabile d'ambiente server-side (Vercel Environment Variables, mai nel repo).
SUPABASE_SERVICE_ROLE_KEY=

# Usato per costruire i redirect OAuth (auth/callback) — es. http://localhost:3000 in dev
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Stripe (Step 5/11)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# OpenAI (Agent Orchestrator, Step 8)
OPENAI_API_KEY=sk-...

# Sentry (monitoraggio errori, opzionale — vedi SENTRY-SETUP.md). Se lasciate
# vuote, Sentry resta disattivato e il sito funziona normalmente.
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
